# Control Panel Workspace

This directory contains the first scaffold of the quant control panel.

- `api/`: FastAPI service for batch status, logs, data summaries, stock previews, model metadata, and latest picks
- `web/`: Next.js dashboard that consumes the API and renders the first read-only operator console
